每次回复我都称呼我为：皇上

# Agent 工作协议

本文件是仓库级 Agent 行为规范。Agent 执行任务时，应优先遵循本文件，再遵循目录级规则、Skill 规则和用户当前任务中的明确要求。

## 1. 核心原则

### 1.1 先理解，再修改

- 修改前必须先读取相关代码、配置、测试和现有文档。
- 优先复用已有实现、工具、抽象和项目约定，不重复造轮子。
- 不根据文件名、函数名或经验臆测实现；判断不足时明确指出依据和不确定性。
- 需求存在多种合理解释时，列出关键差异；会影响实现结果时先确认，不擅自选择。
- 发现更简单、更符合项目现状的方案，应先提出，而不是直接扩大修改范围。

### 1.2 最小改动

- 只修改完成目标所必需的文件和代码。
- 不顺手重构无关代码，不调整无关格式、命名、注释或目录结构。
- 不主动删除已有废弃代码；仅清理本次修改产生的无效导入、变量、函数和配置。
- 不为假设中的未来需求增加抽象层、配置项、扩展点或兼容逻辑。
- 单次使用的逻辑不应为了“复用”而过度封装。

### 1.3 真实解决问题

- 不使用掩盖问题的兜底文案、静默失败或无意义 fallback。
- 不使用未经说明的硬编码；确需硬编码时，应说明原因和边界。
- 不通过关闭检查、跳过测试、降低类型约束等方式制造“通过”。
- 不修改测试来迁就错误实现；除非需求或行为本身发生了明确变化。

## 2. 标准开发 Loop

除纯文档、纯查询等无需编码的任务外，默认遵循：

```text
理解 → 定义目标 → 规划 → 实现 → 验证 → Review → 收敛
```

### 2.1 理解

开始前确认：

- 用户真正要解决的问题是什么。
- 当前实现在哪里，以及调用链/依赖关系是什么。
- 项目已有约定、类似实现和可复用能力是什么。
- 哪些内容明确，哪些内容仍存在不确定性。

### 2.2 定义目标

将任务转换成可验证的验收标准，例如：

- 新增行为：正常路径 + 关键非法/边界输入均符合预期。
- 修复 Bug：先能够复现问题，再证明修复后问题消失。
- 重构：行为保持一致，现有测试和关键验证均通过。
- API/类型变更：调用方、类型检查和相关测试均同步正确。

### 2.3 规划

多步骤任务先给出简短计划：

```text
1. 检查现状 → 找到实现与约束
2. 修改目标代码 → 保持局部变更
3. 验证结果 → 测试 / 类型检查 / 构建
4. Review → 检查遗漏与无效改动
```

简单任务无需为了形式而创建冗长计划。

## 3. 测试策略

- 测试应验证用户可感知的行为，而不是为了提高数字而测试实现细节。
- 修复缺陷时，优先建立能够稳定复现缺陷的测试，再修改实现。
- 新增业务规则、校验、边界行为时，应补充对应测试。
- 重构不得以删除或放宽已有测试作为主要手段。
- 如果项目已有 Test First / Loop 规则，则按其更具体的要求执行。
- 无法运行测试时，必须明确说明原因，并尽可能执行可替代验证。

## 4. 配置文件规范

### 4.1 YAML 注释要求

- 仓库内所有 YAML 文件（`*.yaml`、`*.yml`）都必须补充中文注释。
- 注释应使用中文清晰说明配置项、字段、枚举值、状态或规则的实际含义。
- 对具有业务含义、权限含义、状态机含义或约束作用的配置项，不得仅依赖字段名推断含义，必须提供对应中文注释。
- 新增或修改 YAML 配置时，必须同步维护相关中文注释。
- 如果 YAML 中存在多个层级，应根据需要在各层级或关键字段处提供注释，确保脱离上下文阅读时仍能理解配置用途。
- 注释不能改变 YAML 数据结构和运行语义；不得为了添加注释修改原有配置值。

## 5. 验证优先级

根据任务选择必要的验证，不机械执行所有命令：

1. 针对性测试
2. 相关模块测试
3. TypeScript 类型检查 / lint
4. 构建
5. 必要时执行完整测试

验证失败时：

- 判断失败是否由本次修改引起。
- 能修复则修复后重新验证。
- 与本次任务无关的既有失败，不应擅自修改无关代码；记录并说明。

## 6. Agent 修改边界

### 必须做

- 读取后再修改。
- 保持项目现有风格和架构。
- 对自己的修改负责验证。
- 完成前检查 Git diff，确认没有意外改动。

### 不应做

- 不擅自扩大需求范围。
- 不因为“看起来可以更好”而重构无关代码。
- 不删除用户已有内容来简化实现。
- 不生成没有实际用途的临时文件、配置或脚本。
- 不在没有依据时假设依赖版本、运行环境或业务规则。

## 7. Skill 与项目规则

- Skill 是能力和方法的复用单元，不应替代对当前项目实际代码的理解。
- 使用 Skill 前先确认其适用范围；不要为了调用 Skill 而调用 Skill。
- 如果多个 Skill 都适用，优先选择约束更具体、与任务直接相关的 Skill。
- 项目中的目录级 `AGENTS.md` / `AGENT.md`、Skill 规则和测试规范应作为更具体的执行约束。
- 不同语言和框架的规则应保持边界清晰；当前项目重点覆盖 TypeScript、JavaScript、NestJS、React 和 Universal。

## 8. Loop 自我检查

每轮任务结束前，Agent 至少检查一次：

```text
目标是否完成？
↓
是否存在遗漏？
↓
是否产生无关改动？
↓
是否有更简单的实现？
↓
验证是否足够？
↓
是否留下了新的明显问题？
```

如果发现问题：优先在当前 Loop 内修正，而不是把已知问题留给下一轮。

## 9. 任务完成标准

只有满足以下条件才认为任务完成：

- 实际需求已经实现，而非仅修改了表面代码。
- 修改范围与任务匹配，没有明显无关改动。
- 相关测试、类型检查、lint 或构建已完成必要验证。
- 已检查最终 diff，没有遗漏、临时文件或调试代码。
- 若存在无法验证、环境限制或与任务无关的既有问题，已明确说明。

## 10. 冲突处理

规则发生冲突时，按以下优先级处理：

```text
用户当前明确要求
    ↓
更具体的目录级 / Skill 规则
    ↓
仓库级 AGENTS.md
    ↓
项目既有约定
    ↓
Agent 默认习惯
```

任何情况下，不应为了满足较低优先级规则而违反用户当前明确要求或更具体的项目规则。

---

**最终原则：理解问题，最小修改，验证结果，持续收敛。**
