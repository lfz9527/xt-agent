# Python Review 规则

仅在项目包含 Python 时加载。

- 检查可变默认参数。
- 检查 None 处理和异常边界。
- 检查资源是否通过 context manager 或等价方式可靠释放。
- 检查 async/await、任务生命周期和阻塞调用。
- 检查类型提示是否与真实运行行为一致。
- 检查重复函数、重复类和重复 helper。
- 注意全局状态、隐式副作用和导入循环。
