# P2-2 Artifact Runtime

P2-2 establishes the run-scoped artifact workspace under `.loop/` for Plan, Spec/PECS, Evidence and Review artifacts.

The implementation is provided by `RunArtifactStore`; Run Runtime exposes Plan creation and synchronizes `planArtifactExists` into runtime facts so the `PLAN -> IMPLEMENT` transition guard can rely on persisted state.
