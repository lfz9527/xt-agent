# TypeScript Review 规则

- 检查不必要的 `any`、危险类型断言和类型逃逸。
- 检查 `null` / `undefined`、联合类型和 narrowing。
- 检查 Promise、async/await、未处理 rejection 和并发。
- 检查泛型是否真正提供约束。
- 检查 ESM/CJS、动态 import 和模块边界。
- 检查 Type、Interface、DTO、Schema 是否重复定义。
