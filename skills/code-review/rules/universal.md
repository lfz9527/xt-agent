# 通用 Review 规则

适用于所有语言。

- 检查逻辑、边界条件、状态转换和错误路径。
- 检查认证、授权、输入校验、注入、敏感信息泄露。
- 检查异步、并发、竞态、取消、超时和资源释放。
- 检查事务、一致性、幂等性、重复写入和异常恢复。
- 判断变化是否需要新增或修改测试，优先覆盖边界和回归场景。
- 关注高复杂度、重复分支、深层嵌套和难验证逻辑。
- 识别语义重复，新代码优先复用已有能力。
- 尊重项目模块边界和依赖方向。
- 不把个人风格偏好当成 Bug。
